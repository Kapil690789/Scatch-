<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <!-- Include the header partial -->
    <%- include('./partials/header') %>

    <!-- Error Handling -->
    <% if (error && error.length > 0) { %>
        <div class="absolute top-5 left-1/2 transform -translate-x-1/2 p-3 rounded-md bg-red-500">
            <span class="inline-block text-white">
                <%= error %>
            </span>
        </div>
    <% } %>

    <!-- Main Content -->
    <div class="w-full h-screen flex px-20">
        <div class="w-1/2 flex items-center justify-center">
            <div class="w-full px-32">
                <h3 class="text-4xl font-bold">Welcome to <span class="text-blue-400 font-semibold">Scatch</span></h3>
                <h4 class="text-2xl mb-5">Create your account</h4>
                
                <!-- Registration Form -->
                <form autocomplete="off" action="/users/register" method="post">
                    <!-- Full Name Input -->
                    <input 
                        class="bg-zinc-100 block w-full px-3 py-2 mb-4 border border-gray-300 rounded-md" 
                        type="text" 
                        placeholder="Full Name" 
                        name="fullname"
                        required>
                    
                    <!-- Email Input -->
                    <input 
                        class="bg-zinc-100 block w-full px-3 py-2 mb-4 border border-gray-300 rounded-md" 
                        type="email" 
                        placeholder="Email" 
                        name="email"
                        required>
                    
                    <!-- Password Input -->
                    <input 
                        class="bg-zinc-100 block w-full px-3 py-2 mb-4 border border-gray-300 rounded-md" 
                        type="password" 
                        placeholder="Password" 
                        name="password"
                        required>
                    
                    <!-- Submit Button -->
                    <button 
                        type="submit" 
                        class="px-5 py-3 rounded-full bg-blue-500 text-white w-full hover:bg-blue-600">
                        Create Account
                    </button>
                </form>
            </div>
        </div>

        <!-- Right Side Image or Content (optional) -->
        <div class="w-1/2 h-screen bg-cover bg-center" style="background-image: url('/path-to-image.jpg');"></div>
    </div>
</body>
</html>
